# 📊 Session 04: Unidimensional Arrays

This session focuses on data structures, specifically **one-dimensional arrays**. It covers array declaration, traversal
using loops, and the implementation of algorithms for sorting, merging, and statistical analysis.

**Skills Demonstrated:**

- Array declaration, initialization, and traversal.
- Statistical calculations (Mean, Variance, Mode).
- Algorithmic logic: Array inversion (swap) and Linear Merge.

---

## 📂 Exercise List

| Class Name                | Description                                                                                                                                                                       |
|:--------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **`Estadistica`**         | Performs statistical analysis on a dataset (manual or random input). Calculates **Mean** ($\bar{x}$), **Variance** ($\sigma^2$), **Standard Deviation** ($\sigma$), and **Mode**. |
| **`ArrayUnidimensional`** | Manages an integer array with a menu interface. Features include **strictly ascending input validation**, array inversion (swap algorithm), and finding **Max/Min** values.       |
| **`MezclaLineal`**        | Implements the **Linear Merge** algorithm to combine two previously sorted arrays into a third sorted array, optimizing the process without using sorting libraries.              |

---
